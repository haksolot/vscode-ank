---
id: LOG-ce98885b8d83
type: log
title: npm ci crashed on all three runners with 'Exit handler never called!', which is npm timing out on
created: 2026-09-04T06:48:09Z
author: mb635291@bid-e613543
scope:
  - package-lock.json
about: TASK-3e279b4aca7c
seq: 0
schema: 4
version: 1
---

 an unreachable host rather than a dependency problem. Every one of the 536 resolved URLs in the lockfile named artifacts.cloud.one.ad, the corporate mirror this machine installs through -- a host that does not resolve outside the network. The mirror proxies npm, so the tarballs and their integrity hashes are the same bytes and only the host had to change. npm swaps the registry host in a resolved URL for the configured one, so the lockfile now installs here through the mirror and on a runner through npmjs.org, from the same file.
