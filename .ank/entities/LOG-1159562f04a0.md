---
id: LOG-1159562f04a0
type: log
title: The unit suite locates the real binary and reads its version, and CI had no ank to find, so it
created: 2026-09-04T06:58:37Z
author: mb635291@bid-e613543
scope:
  - .github/workflows/ci.yml
about: TASK-b8bf53f4b83a
seq: 0
schema: 4
version: 1
---

 failed with exit 9 -- an environment fact, not a failure of the work. Installing @haksolot/ank in the job makes the environment true rather than making the test conditional, and it buys the coverage that matters most: the Windows shim resolution, which is the one place where finding the binary is not trivial and where this extension already had a bug. The version is pinned. The golden fixtures copied from the CLI are what detect contract drift; a floating install would instead turn any upstream release into a red build with nothing to say about it. The linux artifact is a musl build with no libc constraint declared, so npm installs it on a glibc runner and the static binary runs.
